# Startup Enterprise — secrets template (SAFE TO COMMIT)
# Copy this file to SECRETS.txt on each machine and fill in real values.
# NEVER commit SECRETS.txt to GitHub.

# ── Frontend (.env) ──────────────────────────────────────────
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YOUR_KEY
VITE_SUPABASE_PROJECT_ID=YOUR_PROJECT_REF

# ── Supabase Dashboard → Auth → Google provider ──────────────
# (Client secret goes ONLY in Supabase, not in .env)
GOOGLE_OAUTH_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-YOUR_SECRET

# ── Google Cloud Console (OAuth client) ───────────────────────
# Authorized JavaScript origins:
#   http://localhost:8080
#   https://YOUR_PRODUCTION_DOMAIN (when deployed)
# Authorized redirect URI (Supabase callback):
#   https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback

# ── Supabase Dashboard → Auth → URL configuration ───────────
# Site URL: http://localhost:8080
# Redirect URLs:
#   http://localhost:8080/auth/complete
#   http://localhost:8080/**

# ── Demo admin (optional — needs bootstrap-admin edge function) ─
ADMIN_LOGIN_USERNAME=admin
ADMIN_LOGIN_PASSWORD=admin123
ADMIN_EMAIL=admin@startup-enterprise.app

# ── Manual payment numbers (shown in app UI) ─────────────────
EASYPAISA_NUMBER=03132790207
EASYPAISA_NAME=Ata ur Rehman
JAZZCASH_NUMBER=03132790207
JAZZCASH_NAME=Muniza Ata

# ── Admin role SQL (run in Supabase SQL editor) ──────────────
# INSERT INTO public.user_roles (user_id, role)
# VALUES ('YOUR_AUTH_USER_UUID', 'admin')
# ON CONFLICT (user_id, role) DO NOTHING;
